# Getting Started

Learn how to install and run SpoTUI.

## Installation

Choose your operating system below to install the SpoTUI CLI.

### Windows
Run the following command in PowerShell:

```powershell
iwr -useb https://raw.githubusercontent.com/SkenSMasteR/SpoTUI/master/scripts/install/windows/install.ps1 | iex
```

### Linux
Run the following command in your terminal:

```bash
curl -fsSL -o install.sh https://raw.githubusercontent.com/SkenSMasteR/SpoTUI/master/scripts/install/linux/install.sh && chmod +x install.sh && ./install.sh
```

## Running SpoTUI

Once installation is complete:

1. Open a **new** terminal session.
2. Type the following command to start SpoTUI:

```bash
spotui
```

After the CLI opens, press `1` to install the SpoTUI theme.

## SpoTUI CLI Preview

![SpoTUI CLI](https://img.ge/i/Ir0T476.png)
