# Sharing Your Theme

Submit your theme at [spotui.root.sx](https://spotui.root.sx/). It will appear in the theme browser for all SpoTUI users.

You can also share a theme directly with anyone using the pull command:

```bash
tui -t pull RWFzdGVyIGVnZw==   # base64 of theme name
```
